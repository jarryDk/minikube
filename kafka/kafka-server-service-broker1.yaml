apiVersion: v1
kind: Service
metadata:
  name: kafka-server-service-broker1
  labels:
    app.kubernetes.io/name: kafka-server-service-broker1
spec:
  ports:
    - port: 9092
      name: plantext
    - port: 9093
      name: ssl
  selector:
    app.kubernetes.io/name: kafka-server-broker1 